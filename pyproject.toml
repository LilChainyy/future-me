[project]
name = "future-me"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ag2[openai,ag-ui]>=0.12.0",
    "python-dotenv>=1.0.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
]

[tool.setuptools.packages.find]
include = ["backend*"]
